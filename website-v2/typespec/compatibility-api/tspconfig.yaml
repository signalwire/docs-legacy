# Compatibility API - outputs to specs/compatibility
extends: ../tspconfig.yaml
emit:
  - "@typespec/openapi3"

options:
  "@typespec/openapi3":
    emitter-output-dir: "{cwd}/../../specs/compatibility"
    output-file: openapi.yaml
    openapi-versions:
      - "3.1.0"
