# ClientFactory

The `ClientFactory` is a class that allows you to create a `SignalWireClient` instance.

```javascript
const client = new ClientFactory();
```

## Parameters

- `apiKey`: The API key to use for the client.
- `apiSecret`: The API secret to use for the client.
- `apiToken`: The API token to use for the client.