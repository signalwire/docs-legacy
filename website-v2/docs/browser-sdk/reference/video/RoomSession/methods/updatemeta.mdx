---
title: "updateMeta"
slug: /js/reference/video/room-session/update-meta
description: updateMeta method for the RoomSession class.
toc_max_heading_level: 3
---

### updateMeta

- **updateMeta**(`meta`): `Promise<void>`

Updates the RoomSession metadata by only setting the specified fields. This is
different from [setMeta](/docs/browser-sdk/js/reference/video/room-session/set-meta), which replaces the whole metadata object.

#### Parameters

<ParamField path="meta" type="Record<string, unknown>">
  The update to the metadata.
</ParamField>

#### Returns

`Promise<void>`

#### Permissions

- `room.set_meta`

You need to specify the permissions when [creating the Video Room Token](/docs/apis/video/create-room-token) on the server side.

#### Example

```js
roomSession.on("room.updated", (e) => {
  // We can set an event listener to log changes to the metadata.
  console.log(e.room.meta);
});

await roomSession.setMeta({ foo: "bar", baz: true });
// The logger will now print `{ foo: "bar", baz: true }`

await roomSession.updateMeta({ baz: false, t: 10 });
// The logger will now print `{ foo: "bar", baz: false, t: 10 }`
```

